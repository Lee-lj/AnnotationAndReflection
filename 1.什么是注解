注解(annotation)的作用
1.不是程序本身，可以对程序作出解释(这一点跟注释comment一样)
2.可以被其他程序(例如编译器)读取
注解的格式:
以@注释名 在代码中存在，还可以添加数值，例如@RequestMapping(value="xxx")

注解通常在哪里使用:
可以附加在package，class，method，field等方法上，相当于给他们添加了额外的辅助信息，可以通过反射机制编程实现对元数据的访问
